{- FIXME: Should be able to do `monoidal.{ arrow, op }`, but Dhall doesn’t like
          that for some reason.
-}
  λ(kArrow : Kind → Kind → Kind)
→ λ(kProduct : Kind → Kind → Kind)
→ λ(vObject : Kind)
→ λ(object : Kind)
→ λ(monoidal : ./Kind kArrow kProduct vObject object)
→   { constraint =
        monoidal.constraint
    , arrow =
        monoidal.arrow
    , op =
        monoidal.op
    }
  : ./../Semigroupal/Kind kArrow kProduct vObject object
