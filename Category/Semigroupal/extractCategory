{- FIXME: Should be able to do `semigroupal.{ arrow, op }`, but Dhall doesn’t like
          that for some reason.
-}
  λ(kArrow : Kind → Kind → Kind)
→ λ(kProduct : Kind → Kind → Kind)
→ λ(vObject : Kind)
→ λ(object : Kind)
→ λ(semigroupal : ./Kind kArrow kProduct vObject object)
→   { constraint = semigroupal.constraint, arrow = semigroupal.arrow }
  : ./../Kind kArrow kProduct vObject object
