    let kArrow = ./../Function/Kind

in  let kProduct = ./../Tuple/Kind

in    λ(object : Kind)
    → λ(arrow : kArrow (kProduct object object) Type)
    → λ(a : object)
    → λ(b : object)
    → { to : arrow { _1 = a, _2 = b }, from : arrow { _1 = b, _2 = a } }
