  λ(max : { _1 : Natural, _2 : Natural } → Natural)
→   { additive =
        { identity = λ(_ : {}) → 0, op = max }
    , multiplicative =
        ./monoid
    }
  : ./../Duoid/Type Type ./../Category/Set/monoidal/cartesian Natural
