let cat = ./../Category/Cat/semigroupal

let vObject = Type

in    λ(cObject : Kind)
    → let dObject = Type
      
      in    λ(d : ./../Category/Kind cat vObject dObject)
          → λ(m : cat.arrow cObject dObject)
          → ./../Ran/Type cObject dObject d m m
