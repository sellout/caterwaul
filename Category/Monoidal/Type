let kCat = ./../Cat/semigroupal

let vObject = Type

in    λ(object : Kind)
    → λ(v : ./Kind kCat Type vObject)
    → λ(cat : ./Kind kCat vObject object)
    → { category :
          ./../Type object v (./extractCategory kCat vObject object cat)
      , monoid :
          ∀(m : object) → ./../../Monoid/Type object cat m
      }
