let kArrow = ./../Function/Kind

let kProduct = ./../Tuple/Kind

in    λ(object : Kind)
    → let profunctor = kArrow (kProduct object object) Type
      
      in  ./../Semigroup/Type
          profunctor
          (./../Category/Profunctor/semigroupal object)
