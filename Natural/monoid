let cat = ./../Category/Cat/semigroupal

let vObject = Type

let Set =
      ./../Category/Monoidal/Cartesian/extractMonoidal
      cat
      vObject
      Type
      ./../Category/Set/monoidal/cartesian

in    { unit =
          λ(_ : {}) → 0
      , product =
          λ(t : { _1 : Natural, _2 : Natural }) → t._1 + t._2
      }
    : ./../Monoid/Type Type Set Natural
